import { useContext} from 'react';
import React from 'react';
import "./Topbar.css";

const Topbar = (props) => {

    return (
        <div className='comp-topbar'>
            BLA BLA BLA
        </div>
    )
};

export default Topbar;